/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'i.ibb.co', 'th.bing.com',"www.jungle.lk"], // Add allowed domains here
    },
};

export default nextConfig;
